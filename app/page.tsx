import Link from 'next/link';
import { liga } from '@/lib/liga';
import { PrimeiraTemporada, Duvidas } from '@/components/primeira-temporada';
import { getTabela, getTemporada, getRodadas, getPainelCapitao } from '@/lib/data';
import { nomeTemporada } from '@/lib/format';
import { Tabela } from '@/components/tabela';
export default async function Inicio() { const [t,tabela,rodadas,painel] = await Promise.all([getTemporada(),getTabela(),getRodadas(),getPainelCapitao()]); const emDia = painel.elenco.filter(j => j.podeEscalar).length; return <><div className="hero"><h1>{nomeTemporada(t)}</h1><p>o primeiro campeonato de várzea virtual</p></div><section className="tabela-inicio" aria-label="Classificação da temporada"><div className="secao-cabecalho"><h2>Classificação de exemplo</h2><span><i className="ponto"/> {rodadas.filter(r => r.status === 'encerrada').length}ª rodada encerrada</span></div><Tabela linhas={tabela}/></section><PrimeiraTemporada/><section className="como-funciona"><h2>Dentro de campo, é com vocês.</h2><div className="tres-colunas"><div><span className="numero-etapa">01</span><h3>Onze de cada lado.</h3><p>Seu clube, seus amigos e uma posição para cada um. Futebol no videogame, no modo Clubs.</p></div><div><span className="numero-etapa">02</span><h3>Todo ponto tem peso.</h3><p>Duas rodadas durante a semana, premiação do primeiro ao quinto colocado e Copa Várzeão após a liga.</p></div><div><span className="numero-etapa">03</span><h3>Organização fora dele.</h3><p>Tabela, rodadas e elenco no mesmo lugar. Regras claras do primeiro ao último apito.</p></div></div></section><section className="chamada-capitao"><div><h2>Capitão, seu elenco<br/>em ordem.</h2><p>O capitão recolhe as contribuições dos jogadores e paga a mensalidade do time à plataforma. O painel ajuda a acompanhar quem acertou sua parte.</p><Link className="botao" href="/capitao">Conhecer o painel do capitão <span aria-hidden="true">↗</span></Link></div><div className="previa-painel" aria-label="Exemplo de controle do elenco"><span>Elenco em ordem</span><div><strong>{painel.elenco.length}</strong><span>no elenco</span><strong>{emDia}</strong><span>em dia</span></div><p><span className="texto-alerta">● {painel.elenco.length - emDia} mensalidades pendentes</span><br/>Você sabe quem pode entrar em campo.</p></div></section><Duvidas/><section className="contato"><div><h2>Uma das 20 vagas pode ser sua.</h2><p>Faça a pré-inscrição. O campeonato começa quando as equipes estiverem fechadas.</p></div><div><a className="botao" href={liga.preInscricaoUrl} target="_blank" rel="noopener noreferrer">Pré-inscrição do time ↗</a><small>WhatsApp da organização · (48) 98871-6244</small></div></section></>; }
